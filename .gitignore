# Reverse engineering binaries (extracted frameworks, Ghidra projects)
re/app/
re/designer/

# Legacy RE capture dirs (curated copies in re/processors/)
re/phoenix/
re/caseta-uart/
re/rr-sel-uart/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
ENV/

# Data directory (captures, projects, certs)
data/
# Tracked data exceptions (research artifacts, not system-specific)
!data/device-fingerprints.json
!data/phoenix-ssl/
!data/wiz-device-serials.json


# ESPHome build artifacts
rf/esphome/.esphome/
rf/esphome/**/.pioenvs/
rf/esphome/**/.piolibdeps/

# ESPHome secrets
rf/esphome/secrets.yaml

# External tools (not part of this repo)
rtl_433/

# Node.js
node_modules/
*.tsbuildinfo

# macOS
.DS_Store
._*

# IDE
.idea/
.vscode/
*.swp
*.swo

# Raw capture files (in case any escape data/)
*.raw
*.cu8
*.pcapng
*.log

# RE processor captures (override *.log ignore)
!re/processors/*.log

# Packet capture / recording output
*.jsonl

# Lutron project files (in case any escape data/)
*.ra3
*.hw
*.lut
*.mdf
*.ldf
*.pem

# Certificates and keys (private keys, PFX bundles)
certs/
*.pfx
*.p12
*.csr
*.srl
*.crt

# Metadata
.lutron-metadata.json

# CCX sequence counter
.ccx-seq

# Designer project tool state
.designer-project-state.json
.designer-sysfiles1-page.bin

# SQLite databases
*.db

*.app
*.msix
*.pdf
*.har
*.bndb
*.csv
*.vive

.obsidian/
.claude/projects/
.claude/settings.local.json
.mcp.json
config.json

# iOS/Android app binaries
*.ipa
*.apk

# RF captures (large binary files)
captures/

tmp/
/src/
/build/
firmware/build*/

# Firmware secrets (Thread network config)
firmware/src/ccx/thread_config.h
Drivers/
._*

# Claude worktrees
.claude/worktrees/

# .NET build artifacts
tools/auth-bypass/bin/
tools/auth-bypass/obj/
tools/dll-patcher/**/bin/
tools/dll-patcher/**/obj/

# Session memory
.remember/
