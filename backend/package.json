{
  "name": "cca-backend",
  "version": "1.0.0",
  "description": "High-performance packet server for Lutron CCA RF",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun --watch run src/server.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.0.0"
  }
}
