esphome:
  name: pico-trigger
  friendly_name: Pico RF Transmitter

esp32:
  board: esp32dev
  framework:
    type: arduino

wifi:
  ssid: gompper-sage-iot
  password: 7qnUoWuiVbExHaU

logger:
  level: DEBUG

api:
  encryption:
    key: "EixuPCx/wLtc5a55a/16gNEubH7qiZWFhn7LR98qQU8="

ota:
  platform: esphome
  password: "test"

# web_server removed to save memory - use local Python server with aioesphomeapi instead
# See: esphome/esp32_controller.py

# External component for Lutron CC1101
external_components:
  - source:
      type: local
      path: custom_components

# SPI bus for CC1101
spi:
  clk_pin: GPIO18
  mosi_pin: GPIO23
  miso_pin: GPIO19

# Lutron CC1101 component
lutron_cc1101:
  id: lutron_radio
  cs_pin: GPIO21
  gdo0_pin: GPIO2

# RF transmit buttons
button:
  # Pico-style buttons for device 05851117
  - platform: template
    name: "RF On (Pico)"
    id: rf_on
    icon: "mdi:lightbulb-on"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_button_press(0x17118505, 0x02);

  - platform: template
    name: "RF Off (Pico)"
    id: rf_off
    icon: "mdi:lightbulb-off"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_button_press(0x17118505, 0x04);

  - platform: template
    name: "RF Raise (Pico)"
    id: rf_raise
    icon: "mdi:arrow-up-bold"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_button_press(0x17118505, 0x05);

  - platform: template
    name: "RF Lower (Pico)"
    id: rf_lower
    icon: "mdi:arrow-down-bold"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_button_press(0x17118505, 0x06);

  - platform: template
    name: "RF Favorite (Pico)"
    id: rf_favorite
    icon: "mdi:star"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_button_press(0x17118505, 0x03);

  # Bridge-style level commands for device AF902C00
  - platform: template
    name: "Level 0% (AF902C00)"
    icon: "mdi:lightbulb-off-outline"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_level(0x002C90AF, 0);

  - platform: template
    name: "Level 25% (AF902C00)"
    icon: "mdi:brightness-4"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_level(0x002C90AF, 25);

  - platform: template
    name: "Level 50% (AF902C00)"
    icon: "mdi:brightness-5"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_level(0x002C90AF, 50);

  - platform: template
    name: "Level 75% (AF902C00)"
    icon: "mdi:brightness-6"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_level(0x002C90AF, 75);

  - platform: template
    name: "Level 100% (AF902C00)"
    icon: "mdi:brightness-7"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_level(0x002C90AF, 100);

  # PAIRING - Uses 0xB9 format from real Pico capture
  - platform: template
    name: "PAIR (0xB9)"
    icon: "mdi:link-plus"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_pairing_b9(0x17118505);

  # TEST - Single packet for RTL-SDR capture analysis
  - platform: template
    name: "TEST PKT"
    id: test_pkt
    icon: "mdi:access-point"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_test_packet(0x17118505);

  # PAIR with NEW unique device ID for ESP32
  - platform: template
    name: "PAIR ESP32"
    id: pair_esp32
    icon: "mdi:link-variant-plus"
    on_press:
      - lambda: |-
          // Unique device ID for ESP32 (not the real Pico's ID)
          // Using 0xCC110001 = "CC1101" with suffix 01
          id(lutron_radio)->send_pairing_b9(0xCC110001);

  # Scene Pico 084b1ebb buttons (Bright/Entertain/Relax/Off)
  # Scene Picos use button codes 0x08-0x0B, NOT 0x02-0x06 like dimmer Picos
  - platform: template
    name: "Bright (084b1ebb)"
    id: rf_bright_084b1ebb
    icon: "mdi:brightness-7"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_button_press(0x084b1ebb, 0x08);

  - platform: template
    name: "Entertain (084b1ebb)"
    id: rf_entertain_084b1ebb
    icon: "mdi:party-popper"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_button_press(0x084b1ebb, 0x09);

  - platform: template
    name: "Relax (084b1ebb)"
    id: rf_relax_084b1ebb
    icon: "mdi:sofa"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_button_press(0x084b1ebb, 0x0A);

  - platform: template
    name: "Off (084b1ebb)"
    id: rf_off_084b1ebb
    icon: "mdi:lightbulb-off"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_button_press(0x084b1ebb, 0x0B);

  # =====================================================
  # BRIDGE EMULATION - Pairing Beacons
  # These should make dimmers/picos flash as if bridge
  # is in pairing mode!
  # =====================================================

  # Main beacon button - sends 0x92 beacons for 10 seconds
  # Using REAL bridge zone ID: af902c00
  - platform: template
    name: "BEACON (Pairing Mode)"
    id: beacon_pairing
    icon: "mdi:broadcast"
    on_press:
      - lambda: |-
          // Send 0x92 pairing beacons using real bridge zone ID
          id(lutron_radio)->send_beacon(0xAF902C00, 0x92, 10);

  # Quick 5-second beacon for testing
  - platform: template
    name: "BEACON 5s"
    id: beacon_short
    icon: "mdi:broadcast"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_beacon(0xAF902C00, 0x92, 5);

  # Different beacon types for experimentation
  - platform: template
    name: "BEACON 0x91"
    id: beacon_91
    icon: "mdi:radio-tower"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_beacon(0xAF902C00, 0x91, 5);

  - platform: template
    name: "BEACON 0x93"
    id: beacon_93
    icon: "mdi:radio-tower"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_beacon(0xAF902C00, 0x93, 5);

  # =====================================================
  # BRIDGE-STYLE LEVEL COMMANDS
  # Uses bridge zone ID + target dimmer ID in payload
  # Target: dimmer 06fdeff4
  # =====================================================

  - platform: template
    name: "Bridge Level 100% (06fdeff4)"
    id: bridge_level_100
    icon: "mdi:brightness-7"
    on_press:
      - lambda: |-
          // Bridge zone ID: 0xAF902C00, Target: 0x06FDEFF4
          id(lutron_radio)->send_bridge_level(0xAF902C00, 0x06FDEFF4, 100);

  - platform: template
    name: "Bridge Level 50% (06fdeff4)"
    id: bridge_level_50
    icon: "mdi:brightness-5"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_bridge_level(0xAF902C00, 0x06FDEFF4, 50);

  - platform: template
    name: "Bridge Level 0% (06fdeff4)"
    id: bridge_level_0
    icon: "mdi:brightness-1"
    on_press:
      - lambda: |-
          id(lutron_radio)->send_bridge_level(0xAF902C00, 0x06FDEFF4, 0);
